server {
  listen 80;

  location / {
    root /app/build;
    index index.html;
    try_files $uri /index.html;
  }

  error_page 500 502 503 504 /500.html;
  location = /500.html {
    root /app/build;
  }
}
